Rent Device Agent 安装步骤

1. 双击安装包并完成安装。
2. 安装向导会显示“设备绑定”页面，在输入框粘贴管理员在网站设备详情页生成的 6 位访问码。
3. 安装程序会把客户端安装到 Program Files，并注册为 Windows Service，自动开机启动。
4. 访问码只使用一次；绑定成功后，网站上的访问码立即失效。

不需要输入网站地址、设备序列号、Token，也不需要安装 .NET。
注册码绑定成功后立即失效，客户端会自动保存加密凭证并在后台运行。
